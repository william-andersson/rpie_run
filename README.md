# Retropie EmulationStation start script.<br>
Requires xdotool **`sudo apt install xdotool`**<br>
> - [x] Fixes problem with x86 build where EmulationStation starts minimized.<br>
> - [x] Keeps the window active until exit.
